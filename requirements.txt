# 小红书链接转文字 Skill · Python 依赖
# 用法：pip install -r requirements.txt
#
# ── 核心（必装）：下载 + 图文 OCR，纯 pip、无系统依赖 ──
requests>=2.28
beautifulsoup4>=4.12
pytesseract>=0.3.10
pillow>=10.0

# ── 视频转写（可选，额外需要系统装 ffmpeg）──
# pip install openai-whisper
#
# ── 离线兜底（可选）──
# pip install vosk  +  下载 vosk-model-small-cn-0.22 并设 VOSK_MODEL_DIR 环境变量
#
# ── 抖音免 key 下载（主路径，需浏览器二进制）──
playwright>=1.40
#
# ── 反爬兜底 / 抖音下载（可选）──
# pip install playwright && playwright install chromium
#
# ── 系统级依赖（无法用 pip 装，需另行安装）──
# ffmpeg    ：brew install ffmpeg (macOS) / apt install ffmpeg (Linux)  —— 视频转写抽音轨
# tesseract ：brew install tesseract tesseract-lang (macOS)            —— 图文批量 OCR
